import { BaseResource } from "../resources/base";
export declare class Brc20Resource extends BaseResource {
    list({ page, sort, }?: {
        page?: number;
        sort?: "oldest" | "newest";
    }): Promise<{
        tick: string;
        minted: number;
        max_supply: number;
        price: number | null;
    }[]>;
    getInfo({ name }: {
        name: string;
    }): Promise<{
        tick: string;
        minted: number;
        max_supply: number;
        price: number | null;
    }>;
}
//# sourceMappingURL=brc20.d.ts.map