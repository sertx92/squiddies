"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Brc20Resource = void 0;
const base_1 = require("../resources/base");
class Brc20Resource extends base_1.BaseResource {
    async list({ page, sort, } = {}) {
        let url = `/brc20`;
        const params = new URLSearchParams();
        if (sort) {
            params.append("sort", sort);
        }
        if (page) {
            params.append("page", page.toString());
        }
        if (params.size) {
            url += `?${params.toString()}`;
        }
        return this.client.fetch(url);
    }
    async getInfo({ name }) {
        return this.client.fetch(`/brc20/${name}`);
    }
}
exports.Brc20Resource = Brc20Resource;
//# sourceMappingURL=brc20.js.map