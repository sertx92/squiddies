import { BaseResource } from "../resources/base";
import { Inscription, InscriptionTrait } from "../schemas/inscription";
import { InscriptionTransfer } from "../schemas/inscriptionTx";
export declare class InscriptionResource extends BaseResource {
    getInfo(identifier: string | number): Promise<Inscription>;
    list({ sort, before, after, }?: {
        sort?: "inscription_number_desc" | "inscription_number_asc";
        before?: number;
        after?: number;
    }): Promise<Inscription[]>;
    getTransfers({ inscriptionId, page, }: {
        inscriptionId: string;
        page?: number;
    }): Promise<InscriptionTransfer[]>;
    getTraits({ inscriptionId, }: {
        inscriptionId: string;
    }): Promise<InscriptionTrait[]>;
}
//# sourceMappingURL=inscription.d.ts.map